#include <definition.h>
#if AMOAMO_PLATFORM_IS_IOS

#include "Twitter.h"

namespace amoamo {
    
void Twitter::tweet(const char* tweet) {
    CCLOG("void Twitter::tweet(const char* tweet) {");
}
    
}

#endif /* AMOAMO_PLATFORM_IS_IOS */